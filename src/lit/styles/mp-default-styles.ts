import { css } from "lit";

export const defaultStyles = css`

    :host {
        --mp-panel-area-color:  light-dark(#f4f6f6, #283747);
    }

    /* @media (prefers-color-scheme: dark) {
        :root {
            --mp-panel-area-color:  #283747;
        }

    } */
    /* @media (prefers-color-scheme: light) {
        :host {
            --mp-panel-area-color:  #f4f6f6;
        }
    } */

`;
